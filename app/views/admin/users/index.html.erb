<div class="container">
 <div class="row">
  <div class="col-md-6 offset-md-3">
      <p class="pt-5"></p>
      <div class="newuser">
<h3>User一覧管理画面</h3>

<%= link_to "新規追加", new_admin_user_path, class: "btn btn-warning mx-2" %>
</div>
<hr>
 <% @users.each do |user| %>
 
  id:<%= user.id %>
  <p></p>email:<%= user.email %>
  <p></p>
  <%= link_to "編集する", edit_admin_user_path(user) %>
  <%= link_to "削除する", user_session_path(user), method: :delete, data: {confirm: "本当によろしいでしょうか"} %>
  
  <hr>
 <% end %>
</div>
 </div>
 </div>